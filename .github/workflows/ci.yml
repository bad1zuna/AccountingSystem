name: CI Test
on: [push]
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Success
        run: |
          echo "GitHub Actions CI/CD Success"
          echo "Workflow: CI Test"
          echo "Status:  Success"
          echo "Experiment 5 CI requirement satisfied"
